# ──────────────────────────────────────────────────────────────────────────
# Person Re-ID & Tracking — Python dependencies
# ──────────────────────────────────────────────────────────────────────────
# CPU-only PyTorch wheels — significantly smaller download (~200 MB vs ~2 GB).
# DELETE the --extra-index-url line below if deploying to a GPU-enabled
# Space and you want CUDA-accelerated inference.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.38,<2
torch==2.6.0+cpu
torchvision==0.21.0+cpu
ultralytics>=8.2,<9
deep-sort-realtime>=1.3,<2
facenet-pytorch>=2.5,<3
opencv-python-headless>=4.9,<5
pandas>=2.2,<3
numpy>=1.26,<2
Pillow>=10.3,<11
